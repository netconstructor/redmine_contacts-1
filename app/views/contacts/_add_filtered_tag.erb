<% unless @filter_tags.nil? %>
    <span class="tag <%= tag_css_class tag.name %>"><a href="<%= contacts_tagged_path(@filter_tags.map(&:name) + tag.name.to_a) %>"><%= tag.name %></a></span>
<% end %>	
